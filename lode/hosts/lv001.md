# lv001

Homelab node. x86_64.

## Location

`hosts/lv001/`:
- `default.nix`, `configuration.nix`, `hardware-configuration.nix`, `disko-config.nix`, `secrets.nix`
- `services/`: `containers/`, `default.nix`

## Role

Homelab compute node running containers (Podman). Minimal service surface compared to `m920q`.

## Wiring in flake

`nixosConfigurations.lv001` + `homeConfigurations."rocksus@lv001"`. Modules: disko, agenix.

Related: [summary.md](summary.md).
